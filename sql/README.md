# SQL

Esta pasta vai armazenar os scripts SQL do projeto.

Arquivos planejados:

- `schema.sql`: criacao das tabelas analiticas.
- `analysis_queries.sql`: consultas para responder as perguntas principais.

As consultas devem priorizar clareza, filtros bem documentados e resultados que possam ser usados no Power BI.
